Build the complete application described in SPEC.md in this working directory.

Read the entire specification. It is the sole product requirement. Implement the application in Rust. Optimize for a compact, readable, maintainable and resource-efficient result with the specified behavior. Choose the architecture, dependencies, algorithms, code organization, implementation order, supporting UI technologies and visual design yourself. Explain consequential design choices and report measured resource use, including required runtime and browser/helper processes.

Create your own implementation. Do not search for or consult an existing implementation of this application, another candidate's solution, repository history or source outside this directory. Public language, library and protocol documentation and normal package dependencies may be used. Do not infer additional features or compatibility requirements.

Keep SPEC.md and this prompt unchanged. Create and edit project files only inside this directory. Existing installed tools may be used; any tool-managed writes outside this directory must be limited to operator-approved build/package caches. Do not change global configuration, unrelated projects or account settings. Do not push, deploy or publish the result. If the environment prevents a required operation, record the limitation and continue independent work.

Implement and verify the result. Do not weaken requirements, add stubs disguised as working behavior, or substitute mock success for real-provider evidence. Continue until the feasible implementation and checks are complete. Human login and unavailable platform checks must be reported honestly.

Deliver the source, tests, runnable application instructions, README with Not yet proven, and ACCEPTANCE.md with evidence for every acceptance group and protocol case. State the tested platforms, commands, failures and remaining limitations. Choose your own working process; no plan or architecture is supplied.
